import ajax from '../lib/ajax.js'

export default function () {
  // ajax.getTables(returnData)
  // ajax.getPlayers(returnData)
}

const returnData = (data) => {
  // console.log('returned data: ', JSON.parse(data))
}
